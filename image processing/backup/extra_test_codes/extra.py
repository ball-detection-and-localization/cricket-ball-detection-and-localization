#cv2.split( the, hsv_channels )
	#for x in range(29):
		#x=x+16
		#for y in range (36):
			#y=y+16	
			#z=the[x,y]
			#print z

	#leftmost = tuple(cnt[cnt[:,:,0].argmin()][0])
	#rightmost = tuple(cnt[cnt[:,:,0].argmax()][0])
	#topmost = tuple(cnt[cnt[:,:,1].argmin()][0])
	#bottommost = tuple(cnt[cnt[:,:,1].argmax()][0])
	#left=leftmost[0]
	#right=rightmost[0]
	#top=topmost[0]
	#bottom=bottommost[0]
	#cv2.rectangle(frame,(left,top),(right,bottom),(0,255,0),3)
	#min_val, max_val, min_loc, max_loc = cv2.minMaxLoc(res,mask = mask)
 
#a.append(int(z))
	#colorsys.hsv_to_rgb(z[0],z[1],z[2])
	#if (z<20):
	#	c.append(int(x))
	#	d.append(int(y))
	#	e=min(c)
	#	f=min(d)
	#	g=max(c)
	#	h=max(d)
		#p=p+1
	#	print e	
